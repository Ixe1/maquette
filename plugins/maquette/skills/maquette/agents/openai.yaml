interface:
  display_name: "Maquette"
  short_description: "Run the full staged image-guided website workflow."
  default_prompt: "Use $maquette to run the full Maquette workflow: create any missing focused brand kit, then any missing component library or focused component/composite coverage, then the requested page or screen."
