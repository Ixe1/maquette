interface:
  display_name: "Maquette Components"
  short_description: "Build a reusable website component library from an approved design system, guided by focused image-generated component sheets."
  default_prompt: "Use $maquette-components to create or revise a focused core component-sheet image with image_gen, add focused data/composite sheets when needed, then build a reusable website component library and preview gallery from the approved design system."
